My code was written in Python 3.12, but should be compatible with future versions to the best of my knowledge.

To run, first install requirements.txt and then run the main.py file like you would any other python file.